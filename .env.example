# =====================================================
# TrueTrack Configuration
# =====================================================

# -----------------------------------------------------
# Server
# -----------------------------------------------------

# Address the API/UI binds to
TRUETRACK_HOST=127.0.0.1

# Port for the API/UI
TRUETRACK_PORT=8000

# Logging level: debug | info | warning | error
TRUETRACK_LOG_LEVEL=info


# -----------------------------------------------------
# CORS / Frontend
# -----------------------------------------------------

# Comma-separated list of allowed origins
# Use localhost for local UI, or your reverse proxy domain
ALLOWED_ORIGINS=http://localhost:3000


# -----------------------------------------------------
# Storage (optional overrides)
# -----------------------------------------------------

# Root directory for stored music
# Defaults to ~/Music/TrueTrack if unset
# MUSIC_LIBRARY_ROOT=/absolute/path/to/music


# -----------------------------------------------------
# Advanced (leave unset unless you know why)
# -----------------------------------------------------

# Worker polling interval (seconds)
# WORKER_POLL_INTERVAL=0.5
